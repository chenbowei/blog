{
  "name": "astro-6-mdx-cloudflare-pages-blog",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name astro-6-mdx-cloudflare-pages-blog",
    "cf:preview": "npm run build && npx wrangler pages dev dist"
  },
  "dependencies": {
    "@astrojs/mdx": "6.0.3",
    "astro": "6.4.8"
  }
}
